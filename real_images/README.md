# Real Images

The original real meter images are not stored in this repository due to size limits.

Source annotations and image references are provided in:
- `export-coco-2019.json`

This COCO file contains image URLs and metadata for reconstructing the dataset.
If the original URLs are still accessible, use `scripts/download_real_images.py` to download them.
